const Navbar = () => {
  return (
    <nav className="w-full bg-[#1a1a2e] text-white px-6 py-3 flex items-center justify-between text-sm">
      <span className="font-semibold tracking-wide">Modrinth to ZIP</span>
      <div className="flex items-center gap-4 text-gray-300"></div>
    </nav>
  );
};

export default Navbar;
